class ReturnOrderHandler:
    @staticmethod
    def return_order_handler(user, message):
        # return all the order ids that are eligible to return
        # rank the order by matching the message
        return